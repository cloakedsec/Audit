package com.audit.datasource.controller;


import org.springframework.stereotype.Controller;

@Controller
public class EmployeeController {
	
	/*
	 * @Autowired EmployeeRepo repo;
	 * 
	 * @RequestMapping("/") public String home() { return "index.jsp"; }
	 * 
	 * @DeleteMapping("/employee/{aid}") public String deleteEmployee(@PathVariable
	 * int eid) {
	 * 
	 * Employee e = repo.getOne(eid); repo.delete(e); return "deleted"; }
	 * 
	 * 
	 * 
	 * @RequestMapping("/addEmployee") public String addEmployee(Employee employee)
	 * {
	 * 
	 * repo.save(employee); return "home.jsp";
	 * 
	 * }
	 * 
	 * @GetMapping("/employees") public List<Employee> getEmployee(){
	 * 
	 * return repo.findAll(); }
	 * 
	 * @PostMapping(path="/employee", consumes= {"application/json"}) public
	 * Employee saveOrUpdateEmployee(@RequestBody Employee employee) {
	 * repo.save(employee); return employee; }
	 * 
	 * 
	 * 
	 * 
	 * @RequestMapping("/getEmployee") public ModelAndView getEmployee(@RequestParam
	 * int eid) {
	 * 
	 * ModelAndView mv = new ModelAndView("EmployeeList.jsp"); Employee employee =
	 * repo.findById(eid).orElse(new Employee()); mv.addObject(employee);
	 * 
	 * return mv;
	 * 
	 * }
	 */

}